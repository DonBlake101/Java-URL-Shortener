package com.portfolio; import org.junit.jupiter.api.Test; import static org.junit.jupiter.api.Assertions.*; public class RepoTest { @Test void createAndFind() throws Exception { Repo repo = Repo.init(); String code = repo.create("abc123","https://example.com"); assertEquals("abc123", code); assertEquals("https://example.com", repo.find("abc123").orElseThrow()); assertTrue(repo.find("zzz").isEmpty()); } }
